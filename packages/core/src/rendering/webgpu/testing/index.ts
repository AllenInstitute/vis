/**
 * @module testing
 *
 * Internal test infrastructure (mock `GPUDevice`, recording `BufferManager`). Not part of the
 * public API. Test files import it via this barrel.
 */
export * from './mock-device';
