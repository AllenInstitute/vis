/**
 * @module foundation
 *
 * Shared WebGPU primitives that every other module builds on: the `isBranded` discriminator
 * and the zod-validated native WebGPU descriptor schemas / enums.
 */
export * from './brand';
export * from './native-types';
